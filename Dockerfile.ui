FROM node:20-alpine AS build

WORKDIR /app

COPY ui/package.json ui/package-lock.json ./
RUN npm ci

COPY ui/ ./
RUN npm run build


FROM nginx:1.27-alpine

COPY docker/nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/build /usr/share/nginx/html

# Nginx official image runs /docker-entrypoint.d/*.sh on container start
COPY docker/ui-env.sh /docker-entrypoint.d/99-rosetta-env.sh
RUN chmod +x /docker-entrypoint.d/99-rosetta-env.sh

# Defaults to nginx reverse-proxy path (can be overridden in Coolify)
ENV UI_API_BASE=/api

EXPOSE 80


